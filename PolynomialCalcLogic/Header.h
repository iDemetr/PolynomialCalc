#pragma once

#ifndef HEADER_H
#define HEADER_H

#include <conio.h>
#include <string>
#include <iostream>
#include <fstream>		
#include <windows.h>
#include <vector>
#include <stack>
#include <tuple>


enum command { poww = '^', multiplication = '*', division = '/', addition = '+', subtraction = '-' };
const int noOperator = -1;


#endif HEADER_H